# Minimal black dunst — 2026-09-22
# De-themed: flat black, no rounded corners, no frame, default everything else.

[global]
    background = "#000000"
    foreground = "#dddddd"
    frame_color = "#000000"
    frame_width = 0
    corner_radius = 0
    separator_color = "#000000"
