shell fish

# Font: none specified on purpose — kitty's default is font_family "monospace"
# at size 11, which fontconfig resolves to Noto Sans Mono (that's also what
# Noctalia's "JetBrains Mono" fell back to on niri). MesloLGS Nerd Font dropped
# 2026-09-22 — add font_family back if you ever want a specific font.

# niri (and i3 with default_border none) leaves decorations to clients;
# hide kitty's CSD title bar
hide_window_decorations yes
