# Minimal picom — de-themed 2026-09-22
# Compositing + vsync only. No shadows, no fading, no transparency, no
# rounded-corner detection (those were leftover from the themed setup).

backend = "xrender";
vsync = true;
use-damage = true;
